//stosy

// init(st);
// push(st,x);
// pop(st); (=top()+pop())
// empty(st);

//tablicowa implementacja szybsza ale wymaga N

struct Node{
    int tab[100];
    Node *next;
}
void init(stos/kolejka &st){
	st.size = 0;
}

//kolejka

//init(k);
//put(k,x);
//get(k);
//empty(k);

//poczatek kolejki 
